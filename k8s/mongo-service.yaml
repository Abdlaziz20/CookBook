apiVersion: v1
kind: Service
metadata:
  name: cookbook-mongo
  labels:
    app: cookbook-mongo
spec:
  type: ClusterIP
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
    name: mongodb
  selector:
    app: cookbook-mongo

